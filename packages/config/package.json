{
  "name": "@foodime/config",
  "version": "1.0.0",
  "description": "Configuration utilities and constants for Foodime",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run clean && pnpm run build"
  },
  "keywords": ["config", "configuration", "constants", "environment"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@repo/eslint-config": "workspace:*",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "eslint": "^9.27.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.1.6"
  }
}
